console.log(`Hogona discovery and enrichment pipeline

Run one explicit stage at a time:
  npm run discover            Collect Serper Places evidence for Karnataka.
  npm run queue               Create deduplicated enrichment jobs.
  npm run export -- 3         Export 1-3 jobs to the synced Drive folder.
  npm run import              Import and archive completed Drive batches.
  npm test                    Run the unit tests.

See README.md for the full workflow.`);
